# Node SDK (`@aep/otel`)

Alpha goals:

1. Thin wrapper around `openai` or `@langchain/openai`.
2. OTLP HTTP exporter with retry + batch.
3. Shared TypeScript types for AEP core fields.

## TODO

- [ ] `package.json` with TypeScript and pnpm workspace.
- [ ] Jest unit test capturing span structure. 